# Source Code

Result-determining source code belongs here.

Requirements:
- deterministic inputs and outputs where applicable;
- explicit task × method matrix orientation;
- no silent imputation;
- logged solver/software versions;
- no hidden manual edits to generated result tables.

Independent verifier code should live under `verification/`, not here.
